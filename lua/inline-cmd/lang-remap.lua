--- INFO: Some languages support mutliple languages inside of itself.
--- This table should help inline-cmd to know which languages to check.
--- If a language is not defined here,
--- inline-cmd will assume that it only supports itself
--- for example, lua would automatically be:
--- lua = { "lua" }

return {
	markdown = { "markdown", "html" }
}
